# VIN OCR CPU requirements

# Pipeline orchestration / tracking
zenml==0.92.0
mlflow==3.7.0
dvc==3.64.2

# PaddlePaddle (CPU)
paddlepaddle==3.0.0
paddleocr==3.3.3

# Computer Vision & Image Processing
opencv-python==4.13.0.90
opencv-contrib-python==4.10.0.84
Pillow==12.1.0
scikit-image==0.26.0
imageio==2.37.2

# Deep Learning & ML Utilities
numpy==2.4.1
scipy==1.17.0
scikit-learn==1.5.2

# Data augmentation
albumentations==2.0.8
albucore==0.0.24

# HF stack (CPU)
# Note: DeepSeek-OCR requires NVIDIA GPU with CUDA for full support
# On CPU/Apple Silicon, the pipeline will use baseline fallback
torch==2.6.0
torchvision==0.21.0
transformers==4.46.3
tokenizers==0.20.3
accelerate==1.12.0
peft==0.18.1
timm==1.0.24
einops==0.8.0
datasets==3.0.1

# DeepSeek-OCR specific dependencies
addict>=2.4.0
easydict>=1.10

# Flash Attention Stub (required for non-CUDA systems)
# Install manually: pip install -e ./stubs/flash_attn_stub
# This provides stub functions that raise clear errors if called

# Configuration & Data Management
pyyaml==6.0.2
pydantic==2.11.9
pydantic-settings==2.12.0

# Data Processing & Analysis
pandas==2.3.3
rapidfuzz==3.14.3

# Web Interface (Streamlit)
streamlit==1.53.1
plotly==6.5.2
altair==6.0.0

# Utilities & Tools
tqdm==4.67.1
requests==2.32.5
click==8.2.1

# Text processing
python-bidi==0.6.7
shapely==2.1.2
pyclipper==1.4.0

# File operations
lmdb==1.7.5
pypdfium2==5.3.0

# Testing & Development
pytest==9.0.2

# Hyperparameter tuning
optuna>=4.0.0
